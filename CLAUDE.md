# Project Rules

## Pre-commit

Always run `pnpm pre-commit` before suggesting a commit message. This runs formatting, linting, type-checking, and build in sequence. Do not suggest a commit if this command fails.
